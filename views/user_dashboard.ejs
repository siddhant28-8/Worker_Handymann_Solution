<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <title>User Dashboard</title>

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css" crossorigin="anonymous">

  <!-- Swiper CSS -->
  <link rel="stylesheet" href="https://unpkg.com/swiper/swiper-bundle.min.css"/>

  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"/>

  <style>
    body {
      background: linear-gradient(to bottom, #f6f4f8, #9787cd);
    }

        /* Define primary color for consistency */
    :root {
        --primary-purple: #9100FF;
        --dark-background: #1c1c1c;
        --light-text: #f8f9fa;
        }
     
	 /* Top navbar */
	 /* Styling for the Post-Login Dropdown Menu */
   /* Top navbar */
    .navbar {
      background-color: #000 !important;
    }
    .navbar .nav-link{
     color: #9100FF !important;
    }
    .navbar .nav-link:hover, .navbar .nav-item.active .nav-link{
      color: #9100FF !important;
    }
    .navbar-toggler-icon {
      filter: invert(1);
    }
    
    /* Styling for the Post-Login Dropdown Menu Container */
    .dropdown-menu {
        border-radius: 8px; /* Rounded corners for modern look */
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.5); /* Soft shadow */
        padding: 0; /* Remove default padding for full-height items */
        background-color:var(--primary-purple) ;
    }
    
    /* Style for the Header (User Role and Name) */
    .dropdown-header {
        color: #333;
        font-weight: 600;
        padding: 0.75rem 1rem 0.5rem;
        white-space: nowrap; /* Prevents long names from wrapping */
    }
    
    /* Style for individual Dropdown Items (links) */
    .dropdown-item {
        padding: 0.75rem 1rem;
        transition: background-color 0.3s;
    }
    
    /* Hover effect for Dropdown Items */
    .dropdown-item:hover {
        background-color: var(--primary-purple); /* Background hover matching the theme */
        color: white !important; /* Text color change on hover to white */
    }
    
    /* Style for the Logout Button icon spacing */
    .dropdown-logout-btn i {
        margin-right: 8px;
    }
    
    /* Ensure the profile icon itself maintains the purple color in the navbar */
    /* This is important to ensure the icon is always purple even if other nav links are white */
    .nav-item.dropdown .nav-link i {
        color: var(--primary-purple) !important; 
    }

    .navbar-profile-img {
      width: 36px;
      height: 36px;
      border-radius: 50%;
      object-fit: cover;
      border: 2px solid var(--primary-purple);
      background-color: #fff;
      box-shadow: 0 0 6px rgba(145, 0, 255, 0.3);
      transition: transform 0.2s ease;
    }
    
    .navbar-profile-img:hover {
      transform: scale(1.05);
    }

    .row {
      margin-left: 10px;
    }

    .nav {
      background-color: #cccbcd;
      border-radius: 20px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.4);
      padding: 10px 0;
      color: #fff;
    }

    .nav ul {
      list-style: none;
      padding-left: 0;
      margin: 0;
    }

    .nav li {
      padding: 15px 20px;
    }

    .nav li a {
      color: #0c0c0c;
      text-decoration: none;
    }

    .nav li a:hover {
      background: transparent;
      color: #b118f8;
    }

    .nav li a.active {
      background: transparent;
      color: #b118f8;
    }

    #btnn {
      font-size: 40px;
      width: 40px;
      background-color: transparent;
      border: none;
    }

    /* Book Now button */
    .book-btn {
      background: linear-gradient(135deg, #7a00cc, #9100FF);
      color: #fff !important;
      font-weight: 500;
      border: none;
      border-radius: 25px;
      padding: 10px 25px;
      box-shadow: 0 4px 8px rgba(145, 0, 255, 0.3);
      transition: all 0.3s ease-in-out;
    }
    .book-btn:hover {
      background: linear-gradient(135deg, #9100FF, #b44cff);
      box-shadow: 0 6px 12px rgba(145, 0, 255, 0.5);
      transform: translateY(-2px);
    }
    .book-btn:active {
      transform: translateY(1px);
      box-shadow: 0 3px 6px rgba(145, 0, 255, 0.3);
    }

    .navbar-brand img {
      height: 40px;
    }

    .content {
      display: flex;
      padding: 20px 20px 20px 10px;
      flex-wrap: wrap;
    }

    .card-section {
      flex: 1;
      padding: 10px;
      min-width: 300px;
    }

    .card {
      height: 100%;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.5);
      padding: 20px;
      background-color: #cccbcd;
    }

    /* Profile Image */
    #profile-card .pr-3.pl-2 {
      width: 200px;
      height: 200px;
      flex-shrink: 0;
    }
    #profile-card img.profile-img {
      width: 180px;
      height: 180px;
      max-width: 40vw;
      max-height: 40vw;
      object-fit: cover;
      border-radius: 50%;
      display: block;
      margin: 0 auto;
      flex-shrink: 0;
    }
    #profile-card p {
      margin-bottom: 10px;
    }

    #edit-btn {
      box-shadow: 0 4px 8px #9100FF66;
      transition: box-shadow 0.3s ease;
    }
    #edit-btn:hover {
      box-shadow: 0 6px 12px #9100FF88;
    }

    /* Services Card */
    .services-card {
      background: #cccbcd;
      border-radius: 15px;
      padding: 25px;
      box-shadow: 0 6px 12px rgba(0,0,0,0.5);
      transition: all 0.3s ease-in-out;
    }
    .services-card h2 {
      font-size: 22px;
      font-weight: 600;
      margin-bottom: 20px;
      color: #4b0082;
      border-bottom: 2px solid #9100FF;
      display: inline-block;
      padding-bottom: 5px;
    }

    .service-item {
      display: flex;
      align-items: center;
      justify-content: space-between;
      background: #f5ebeb;
      border-radius: 15px;
      padding: 12px 18px;
      margin-bottom: 15px;
      box-shadow: 0 3px 6px rgba(0,0,0,0.08);
      transition: all 0.3s ease-in-out;
      cursor: pointer;
    }
    .service-item:hover {
      transform: translateY(-4px) scale(1.01);
      box-shadow: 0 6px 14px rgba(145,0,255,0.25);
      background: linear-gradient(135deg, #faf5ff, #f3eaff);
    }
    .service-item img {
      border-radius: 12px;
      margin-right: 15px;
      width: 55px;
      height: 55px;
      object-fit: cover;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
    }
    .service-info {
      flex-grow: 1;
      font-size: 16px;
      color: #333;
      font-weight: 500;
    }

    /* Book Again Button */
    .book-again-btn {
      background: linear-gradient(135deg, #9100FF, #b44cff);
      color: #fff !important;
      font-weight: 500;
      border: none;
      border-radius: 20px;
      padding: 6px 16px;
      font-size: 13px;
      box-shadow: 0 3px 6px rgba(145, 0, 255, 0.3);
      transition: all 0.3s ease-in-out;
    }
    .book-again-btn:hover {
      background: linear-gradient(135deg, #7a00cc, #9100FF);
      transform: translateY(-2px);
      box-shadow: 0 5px 10px rgba(145, 0, 255, 0.5);
    }
    .book-again-btn:active {
      transform: translateY(1px);
      box-shadow: 0 2px 4px rgba(145, 0, 255, 0.3);
    }

    /* Responsive */
    @media (max-width: 768px) {
      .content {
        flex-direction: column;
        padding: 15px;
      }
      #profile-card .pr-3.pl-2 {
        width: 100px;
        height: 100px;
      }
      #profile-card img.profile-img {
        width: 30vw;
        height: 30vw;
      }
      #profile-card .d-flex {
        flex-direction: column;
        align-items: center;
        text-align: center;
      }
      #profile-card .flex-grow-1 {
        margin-top: 10px;
      }
      .card-section {
        padding: 10px 0;
      }
    }

        /* --- Footer Styling --- */
        .footer {
            background-color: var(--dark-background);
            color: white;
            padding: 40px 10px;
        }
        .footer-container {
            display: flex;
            justify-content: space-between;
            flex-wrap: wrap;
            max-width: 1200px;
            margin: auto;
        }
        .footer-left {
            flex: 1;
            min-width: 200px;
            margin: 10px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            border-right: 1px solid #706f6f;
        }
        .footer-left img {
            height: auto;
            width: auto;
            margin-bottom: 5px;
        }

        .footer-center{ 
            flex: 1;
            min-width: 200px;
            margin: 10px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            border-right: 1px solid #706f6f;
        }
        .footer-center ul { 
            list-style: none; 
            padding: 0; 
            line-height: 2.2; 
        }
        .footer-center ul li a { 
            text-decoration: none; 
            color: white; 
            font-size: 14px; 
            transition: color 0.3s; 
        }
        .footer-center ul li a:hover { 
            color: var(--primary-purple); 
        }

        .footer-right { 
            flex: 1;
            min-width: 200px;
            margin: 10px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            gap: 30px; 
        }
        .footer-right .social { 
            display: flex; 
            align-items: center; 
            font-size: 14px; 
        }
        .footer-right .social span { 
            margin-right: 10px; 
            color: #ccc; 
            min-width: 70px; 
            text-align: right;
        }
        .footer-right .social i { 
            font-size: 16px; 
            color: var(--primary-purple); 
            background: white; 
            border-radius: 4px; 
            padding: 5px; 
            transition: transform 0.3s; 
            margin-left: 10px; 
        }
        .footer-right .social i:hover { 
            transform: scale(1.1); 
        }
        .tagline { 
            font-size: 13px; 
            color: #ccc; 
            margin-top: 8px; 
        }
    @media (max-width: 992px) {
            .footer-left, .footer-center, .footer-right { border-right: none; margin-bottom: 20px; }
        }
    @media (max-width: 480px) {
      #profile-card img.profile-img {
        width: 35vw;
        height: 35vw;
      }
    }
  </style>
</head>
<body>

  <!-- Header -->
<div class="container-fluid px-0">
    <nav class="navbar navbar-expand-lg">
        <a class="navbar-brand" href="/">
            <img src="logo.png" alt="HomeWork Logo">
        </a>
        
        <button class="navbar-toggler" type="button" data-toggle="collapse"
            data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false"
            aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ml-auto align-items-center">
                <li class="nav-item active px-3">
                    <a class="nav-link" href="/">Home</a>
                </li>
                <li class="nav-item px-3">
                    <a class="nav-link" href="/pricing">Pricing</a>
                </li>
                <li class="nav-item px-3">
                    <a class="nav-link" href="/About">About</a>
                </li>
                <li class="nav-item px-3">
                    <a class="nav-link" href="/Contact">Contact</a>
                </li>
                <li class="nav-item px-3">
                    <a class="nav-link" href="/Faq">FAQs</a>
                </li>
                
                <li class="nav-item dropdown px-3">
                    <a class="nav-link" href="#" id="profileDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" style="font-size: 24px; padding: 0.375rem 0.5rem;">
                      <% if (user.picture && user.picture.trim() !== '') { %>
                           <!-- ✅ Show uploaded profile image -->
                           <img src="<%= user.picture %>" 
                                alt="Profile" 
                                class="navbar-profile-img">
                         <% } else { %>
                           <!-- ✅ Default circle icon if no image -->
                           <i class="fas fa-user-circle" style="color: var(--primary-purple);"></i>
                      <% } %>
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="profileDropdown">
                        <h6 class="dropdown-header" id="userRoleName">UserName: <%= user.fullname %></h6> 
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item dropdown-logout-btn" href="/logout">
                            <i class="fas fa-sign-out-alt"></i> Logout
                        </a>
                    </div>
                </li>
                </ul>
        </div>
    </nav>
</div>

  <!-- Main Section -->
  <div class="container-fluid">
    <div style="display: flex; justify-content:right;">
      <a href="/book_services" class="btn book-btn" style="margin-top:20px;margin-right:35px;">Book now</a>  
    </div>

    <div style="display: flex; justify-content:start;">
      <button id="btnn">&lt;</button>
    </div>

    <div class="row" style="display: flex-wrap; justify-content:center;align-items:center">
      <!-- Sidebar -->
      <aside class="col-md-2" id="first" style="margin:10px 0">
        <div class="nav">
          <ul>
            <li><a href="user_edit_profile.html"><img src="pencil.png" width="19" height="19" /> <b>Edit Profile</b></a></li>
            <li><a href="/user_history"><img src="time.png" width="20" height="20" /> <b>History</b></a></li>
            <li><a href="/user_notification"><img src="notification.png" width="20" height="20" /> <b>Notification</b></a></li>
            <li><a href="/user_bookings"><img src="order-delivery.png" width="20" height="20" /> <b>Bookings</b></a></li>
            <li><a href="/"><img src="quit.png" width="20" height="20" /> <b>Logout</b></a></li>
          </ul>
        </div>
      </aside>

      <!-- Main Content -->
      <main class="col-md-10" id="main-content">
        <div class="content">
          <!-- Profile Card -->
          <div class="card-section">
            <div id="profile-card" class="card h-100">
              <div class="d-flex align-items-start mb-4 flex-wrap flex-md-nowrap">
                <!-- Profile Image -->
                <div class="pr-3 pl-2">
                <img src="<%= user.picture %>" alt="Profile" class="profile-img" />
                </div>

                <!-- User Info -->
                <div class="flex-grow-1">
                  <p>Name: <b><%= user.fullname %></b></p>
                  <p>Email: <b><%= user.email %></b></p>
                  <p>Contact: <b><%= user.contact %></b></p>
                  <p>Member Since: <b>28 July 2023</b></p>
                  <p>From: <b><%= user.landmark %>, <%= user.city %>, <%= user.state %></b></p>
                </div>
              </div>

              <div class="text-center">
                <button id="edit-btn" class="btn btn-dark">Edit Profile</button>
              </div>
            </div>
          </div>

          <!-- Services Card -->
          <div class="card-section">
            <div class="services-card card h-100">
              <h2>Frequently Booked Services</h2>
              <% if (user && user.history.length > 0) { %>
                <% for (let i = 0; (i < user.history.length && i < 2); i++) { %>
                  <div class="service-item">
                    <img src="<%= user.history[i].picture %>" alt="Service" />
                    <div class="service-info"><%= user.history[i].issue_name %></div>
                    <a href="/book_services?service=<%= user.history[i].issue_name %>" class="btn book-again-btn">Book Again</a>
                  </div>
                <% } %>
              <% } else { %>
                <p style="color:#555;">No recent services booked yet.</p>
              <% } %>
            </div>
          </div>
        </div>
      </main>
    </div>
  </div>

      <footer class="footer">
        <div class="footer-container">
            <div class="footer-left">
                <img src="logo.png" alt="HomeWork Logo">
                <p class="tagline">Book your trusted handyman in just a few clicks.</p>
            </div>

            <div class="footer-center">
                <ul>
                    <li><a href="/About">About</a></li>
                    <li><a href="Signin.html">Sign In</a></li>
                    <li><a href="/Faq">FAQs</a></li>
                    <li><a href="/Pricing">Pricing</a></li>
                    <li><a href="/Contact">Contact</a></li>
                </ul>
            </div>

            <div class="footer-right">
                <div class="social">
                    <span>@homvices</span><a href="#"><i class="fab fa-linkedin-in"></i></a>
                </div>
                <div class="social">
                    <span>@homvices</span><a href="#"><i class="fab fa-facebook-f"></i></a>
                </div>
                <div class="social">
                    <span>@homvices</span><a href="#"><i class="fab fa-instagram"></i></a>
                </div>
            </div>
        </div>
    </footer>


  <script>
    let btnn = document.getElementById("btnn");
    let divi = document.getElementById("first");
    let mainContent = document.getElementById("main-content");

    btnn.onclick = function () {
      if (divi.style.display === "none") {
        divi.style.display = "block";
        mainContent.classList.remove("col-md-12");
        mainContent.classList.add("col-md-10");
        btnn.innerHTML = "<";
      } else {
        divi.style.display = "none";
        mainContent.classList.remove("col-md-10");
        mainContent.classList.add("col-md-12");
        btnn.innerHTML = ">";
      }
    };
  </script>
  
  <!-- ✅ Required scripts for dropdown to work -->
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.min.js"></script>
</body>
</html>
